<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.ge.mapper.UserMapper">
    <resultMap id="userResultMap" type="com.ge.domain.User">
        <id column="id" property="id" jdbcType="BIGINT"/>

        </resultMap>

<insert id="insert" useGeneratedKeys="true" keyProperty="id">
		insert into User  (  values( 
	</insert>

<delete id="delete">
		delete from User where id =#{id}
	</delete>

<update id="update">
		update User set  where id=#{id}
	</update>

<select id="select" resultMap="userResultMap">
		select u.id from user where u.id=#{id}
	</select>

<select id="queryTotalCount" resultType="int">
		select count(u.id) from User u
	</select>

<select id="queryListData" resultMap="userResultMap">
select u.id from user u
<if test="pageSize>=0">
    limit #{currentPage},#{pageSize}
</if>
</select>
